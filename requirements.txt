# Core dependencies (required for all stages)
numpy>=1.21.0
torch>=1.9.0
transformers>=4.21.0          # SPLADE model loading (Stage 1)
sentence-transformers>=2.2.0  # Dense encoding (Stage 2)
tqdm>=4.62.0                  # Progress bars
pyyaml>=6.0                   # Config file loading
python-dotenv>=1.0.0          # .env file support

# Stage 3 — install whichever provider you use
openai>=1.0.0                  # Stage 3 NQ-Tables (text-embedding-3-large/small)
google-generativeai>=0.3.0    # Stage 3 OTT-QA (gemini-embedding-001)

# Optional but recommended
pandas>=1.3.0                  # Recall summary CSV display
matplotlib>=3.5.0              # Recall@k plots (notebooks)

# Jupyter notebook support (for the interactive scripts)
jupyter>=1.0.0

# Development and testing
pytest>=7.0.0